我想创建一个虚拟炒股平台网站，有以下需求：
1，注册人员及群组管理
1.1，允许人员注册后使用，初始化设定有100000人民币等值资金，人员注册后加入到一个群组
1.2，群组内人员可以查看任意成员的交易行为，包括查看买卖情况以及
1.3，运行在后台管理中设定不同的群组
1.4，群组可以实时显示群组内人员资金情况，并作排名

2，股市操作
2.1 获取股市非实时信息，了解前一天收市前中国A股，中国香港股市，美国股市行情信息，需要选择一个合适稳定的行情来源
2.2 个人可以买入或者卖出股票，计算收益或者亏损，每天只能基于前一天收市的行情进行虚拟股票买卖
2.3 提供个人统计功能，统计交易内容，收益情况，可按天，按周，按月统计，也可实时统计

3，后台管理
3.1 提供后台管理页面
3.2 完成群组设定，管理功能。
3.3 后台可对群组成员进行删除，禁止交易

架构：
前端采用vue架构，后台架构Node.js+MySQL,数据库记录交易，人员，群组，资金，股市历史数据等内容
数据库的设计需要和我讨论，确认数据字典。

其他细节需要进一步讨论

2026-4-25

1，初始阶段考虑每次交易收取千分之五的交易佣金，买卖均收取，佣金从账户余额中扣取，或者从卖完股票后的收入中扣取
2，交易的佣金可以在后台设置，单独保存在数据库中，允许买卖佣金的比例不同,A股，港股，美股的佣金比例都可以单独设定，
3，查询历史交易时，需要将佣金列单独罗列出来

考虑增加K线图，需要：

1，测试下载A股前10只股票的历史K线图，保存在数据库中
2，在股票交易页面，可以显示K线图

现在有2个问题：
1，美股的接口调用经常性失败，能否换用其他接口
2，请将所有股和港股的K线数据下载

收益统计的页面里有几个错误需要修复，先看看代码，

1，总收益以及收益率的计算逻辑请描述一下，现在看总收益的计算金额是错误的
2，持仓市值和持仓数量的规则逻辑也描述一下，现在这个数据也是错误的
3，交易统计里显示的数据看一下规则，数据是错误的
4，交易记录里没有交易日期和收益情况描述
另外在持仓查询中，应该描述的是一只股票的成本价格，需要按照下面的逻辑实现：
1，购买股票，其成本价格应该是（股票交易价格+佣金）/购买的股数
2，如果有多次购买同一只股票，其成本价格应该是 累加每次购买的成本然后除以总的购买股数

浮盈亏和实际收益（亏损）的实现，先讨论一下项目中盈亏的设定，分为两种：
1，浮盈亏，表示按照当前持有股票价格交易后（尚未交易），整体的收益亏损数据，需要考虑佣金的情况，与初始资金700万人民币比较。
2，实际收益（亏损），已经完成交易的股票，扣除佣金后实际的收益，这一部分不计算收益率，只计算按照RMB的实际数额
对于单只股票：
1，在持仓查询页面中，应该用浮盈亏和当前盈亏率来表示。
2，如果已经持仓的股票中，再次购买，应该计算平均成本和该股票的整体浮盈亏情况

对于已卖出的股票，是否需要记录单笔交易的"实际收益"（即卖出收益减去买 入成本）？

- 如果是多批次买入再卖出，如何计算？用平均成本法。

前端显示：您希望如何呈现这两种盈亏？

- 统计页面分开显示"浮盈亏"和"已实现收益"
- 交易记录中需要显示每笔卖出的实际收益。

关于已实现收益的计算：当有多次买入、多次卖出时，按平均成本法。例如：

- 第一次买入 100股，成本 10元/股（+佣金）
- 第二次买入 100股，成本 12元/股（+佣金）
- 卖出 150股，按平均成本 (100×10 + 100×12)/200 = 11元 计算，卖出时的佣金卖出金额中扣除

开始准备后台管理界面，需求如下：

* 需要独立 后台管理界面，区别与用户操作界面
* 进入界面需要登录授权，仅被授权的用户可以访问后台管理界面
* 后台管理界面功能点：

  * 用户查看和管理

    * 设定用户组名称，查看或设定用户组有哪些用户
    * 查看所有用户的持仓情况，剩余资金，收益
    * 允许设定用户禁止交易或者允许交易
    * 可以设定用户是否能登录后台查询
    * 可以查看各个用户的登录系统情况（最近1个月），交易情况（缺省为一个月范围内，可按照自定义时间范围选择）
    * 发放注册邀请码
    * 可以删除或者禁用/启用用户，被删除的所有用户数据包括交易持仓等数据均被删除，禁用的用户不允许登录系统
  * 股市设置

    * 允许手动刷新股市数据，和当前时间比较，查看缺失的股市数据，下载数据补齐
    * 设置股市刷新的时间点，可根据不同市场设定不同的刷新时间，后台自动执行数据更新操作
    * 设定允许交易时间范围，可根据不同市场设定不同的交易时间范围，后台可自动执行，用户只有在允许的时间范围内才可以买卖股票
    * 需要能按照不同市场设定执行的佣金比例,，需要记录历史佣金变化情况
  * 统计功能

    * 按照时间范围统计，各个组的收益情况，收益排名，缺省周期范围为1周，可以按照月或者自定义周期统计
    * 统计交易最多的用户，登录次数最多的用户

允许交易时间范围未生效，在非交易时间仍旧允许买卖股票。

在前后端界面，增加关于描述页面，描述当前版本，版权归属暂时写“CAI”，版本号为1.0.0开始，每次变更，最后一位增加1，如有重大变更，会在编译期间要求增加第1位或者第2位的版本号变更。

现在考虑修复项目的一个缺陷，在后台管理界面，点击用户管理，用户列表最右侧的“详情”按钮没有响应，需要修复，我们讨论一下修复方法

在后台增加分红，配股的操作接口，支持手工操作即可。前端将分红给到个人账户余额，配股直接增加持仓人的股票数量。

在管理界面-用户管理页面下，增加删除用户的的按钮，需求如下：
提醒管理员删除用户将删除该用户的所有数据，包括交易数据，在实际删除中也应该清除数据库中所有该用户的相关记录，管理人员确认并输入管理人员密码后才执行下一步动作

需要在用户操作界面增加一个功能，查询资金流水，可按照时间范围查询资金变化（缺省为最近一个月）情况，包括：
1，初始分配增加资金
2，购买股票减少资金
3，购买股票减少佣金
4，卖出股票增加资金
5，卖出股票减少佣金
6，分红资金

需求：

在群组排名页面上，增加群组消息功能，群组内任何人购买，出售股票，分红，配股的消息都会在生成一条群组消息中并展示，自己的消息（记录）用独立的颜色标识。每条群组成员的消息可以有群内成员回复，点赞。在消息下面展示点赞的人，回复消息的人以及回复时间信息。在操作页面首页可以显示未读消息数量，点击未读消息可以跳转至群组排名页面。群组消息可以按照时间筛选，缺省显示最近一个月的消息，消息需要分页展示。

在操作界面，点击 群组排名 按钮需要修复：
1，查看成员交易记录，只显示了股票代码，没有显示股票名称
2，同组其他用户的交易记录查询不到，目前是空的列表
3，查看成员交易记录仅提供最近一个月的交易记录查询，需要在界面描述出来
4，群组消息中，发出来的股票交易消息中只显示了股票代码，需要股票代码，股票名称，单价，数量，总价一起显示

请检查文件夹中项目，为源文件增加注释，要求如下：
1，文件级的注释应该包括文件名称，创建时间，作者（CAISHENG/caisheng.cn@gmail.com）,文件用途描述，版本变更记录；
2，接口和函数级的注释，应该包括：名称，功能，出入参数类型，定义，调用了哪些接口，使用示范；
3，所有注释用英文描述
可以参考业界代码注释规范来完成源代码的注释工作

1，在群组排名页面上，查看成员交易记录框内，增加一个成员选择下拉框，可以查看群组成员的交易记录，同时显示当前选择群组成员的股票持仓情况，资金余额，盈利/亏损情况

2，消息中，要显示股票代码，股票名称，股票市场，单价和总价信息。

20260505 美股和港股数据获得的免费通道：[akfamily/akquant: AKQuant is a high-performance quantitative research and trading framework built on Rust and Python! 开源量化回测框架](https://github.com/akfamily/akquant)

2个问题：
1，消息展示过后，就属于已读消息，不要再显示未读的提示

2，新注册一个用户，没有任何操作就有收益/亏损，请检查问题

TODO

2，增加股票讨论，导师指导，组内操作评论功能

4，增加一个测试人员账号，确认分组功能，组内比较功能等。

5，个人账户变化曲线

2026-5-13

在群组广场页面中，查看成员交易记录，股票持仓表格中，股票的均价和现价应该显示币种，浮盈亏和市值应该换算为人民币。

在收益统计页面中，金额和收益列统一换算为人民币，可以不附加单位，股票价格需要区分是人民币，港币还是美元，需要附带币种单位

2026-5-14

之后通过 http://IP:3006 访问的就是构建后的前端，不再依赖 Vite 开发服务器。
改造总结
项目	改造前	改造后
访问端口	3000（Vite 开发服务器）	3006（后端直接托管）
element-plus.js	2.2MB（未打包单模块）	内联到 index.js 中
首页 JS 总大小	未压缩	1.1MB 原始 / 368KB gzip
gzip 压缩	❌ 无	✅ compression 中间件
开发流程
改代码 → npm run dev (3000端口预览，热更新)
↓ 确认无误
npm run build → 刷新 3006 端口即可

请考虑用AKshare接口替换现有的期权模拟方案，直接在后台保存最近一年的合约数据，当前仅支持中国市场的期权交易，原美股期权交易可以替换。

## 需要确认的问题

### 1. 标的资产范围

当前 whitelist 里有 AAPL/MSFT 等美股。替换后打算支持哪些中国期权标的？

- **ETF期权**: 50ETF / 300ETF / 500ETF / 创业板ETF / 科创50ETF / 深100ETF
- **股指期权**: 沪深300股指期权 / 上证50股指期权 / 中证1000股指期权
- **商品期权**: 豆粕/铜/黄金/白糖等
  建议优先只做 **ETF期权**（与平台现有的A股交易标的匹配度高），你怎么看？

### 2. 合约代码规范

AKShare 的合约代码格式跟现有系统完全不同。现有是 `AAPL20260520C00294800`，AKShare/实盘中是：

- 上交所ETF期权: `510050C2606A03000` 或 `10007976.SH`
- openCTP格式: `510050C2606A03000`（交易所代码 + C/P + 年月 + 行权价）
  是否需要把数据库中 `contract_code` 字段改造适配？还是保留自定义格式但映射到真实合约？

### 3. 非交易时段定价

AKShare 在非交易时段（收盘后、节假日）拿不到实时报价（`option_finance_board` 返回的是当天最后价格）。当前系统全天24小时可交易。你希望：

- **方案A**: 只在交易时段开放期权交易（9:30-11:30, 13:00-15:00）
- **方案B**: 非交易时段用最后收盘价作为参考价继续允许交易
- **方案C**: 非交易时段用简化模型估算价格（类似当前方案）

### 4. 数据同步策略

需要定一个后台同步方案来"保存最近一年的合约数据"：
**初始全量同步**（一次性）：

- 从 `option_contract_info_ctp()` 下载全量合约信息（约2.8万条），筛选ETF期权相关合约
- 对每个活跃合约调用 `option_sse_daily_sina()` 拉取近一年历史日线存入 `option_prices`
  **每日增量同步**（定时任务）：
- 每日开盘前同步合约列表（新上市/退市）
- 每日收盘后更新所有合约最新价格
- 每日检查到期合约进行自动结算
  你觉得这个同步策略合适吗？是否要定时任务自动结算到期的实值期权？

### 5. 币种与账户

现在用户余额是 `user_balance.cash`（兼容多币种），换成纯中国期权后：

- 所有期权交易直接以 **CNY** 计价
- 不再需要 USD/CNY 汇率转换（`utils/currency.js`）
- 但用户现有US股票持仓可能还有USD余额
  你是否希望保留多币种结构（虽然期权只用CNY），还是可以简化？

### 6. 行权规则

当前系统支持美式期权（到期前随时行权）。中国 ETF 期权也是美式，但股指期权是欧式。你希望：

- **统一美式**（所有标的都支持提前行权）
- **按实际规则**（ETF期权美式，股指期权到期自动行权）

### 7. 需要先确认的数据库/网络依赖

AKShare 是 Python 库，而当前后端是 Node.js。现有的股票数据已通过 `child_process` 调用 Python 脚本 (`fetch_kline.py`)。期权数据同样可以用此模式。
需要新安装的 Python 依赖：`pip install akshare pandas`，已经装好了。但需要注意：

- AKShare 的网络请求可能较慢（实测单个接口有时5-10秒）
- 批量拉取全量合约历史数据可能需要较长时间（初始同步可能在分钟级）

1，全部中国期权标的都支持
2，直接改造数据字段适配合约代码格式
3，**方案B**: 非交易时段用最后收盘价作为参考价继续允许交易
4，方案合适，需要定时任务自动结算
5，保留多币种结构
6，按照实际规则
7，OK

## 20260517 增加AI用户

考虑实现以下场景，

1. 每个群组生成3个AI用户，代表保守，随机和激进的投资用户，连接LLM实现在系统中购买或卖出股票操作，操作范围限于A股，港股和美股。在限定金额范围下可以任意操作。AI操作的所有数据来源不限，可以是本地数据库也可以是互联网，交易前允许与LLM做多轮交互操作，需要准备好不同AI用户的上下文。
2. AI用户的目标和普通注册用户一样，追求收益最大化，每天在允许交易的时间段允许任意交易。
3. AI用户在系统中回复自身买入或卖出的逻辑，和普通用户一样，允许同组真实注册用户查看AI用户的交易情况和持仓情况。AI用户也可以回复，点赞配普通用户的交易，但要给出具体的逻辑描述。
4. AI用户不需要有明显的标识，AI用户也可以回复所有真实注册用户的股票购买，发表评论并说明支持或者反对的理由。AI用户在数据库中users表中有记录，需要标记是否时AI用户。
5. 连接的LLM可以在管理界面进行配置，包括LLM的接口地址和APIKEY。
